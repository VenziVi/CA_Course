#include <stdio.h>
#include <math.h>

int main(){
    int p = 10, q;

    printf("Enter an integer: ");
    scanf("%d. &q");
    printf("p/q: %d \n", p, q);

    float n = 10, m, r;

    // printf("Enter an integer: ");
    // scanf("%d. &q");
    // printf("p/q: %d \n", p, q);

    return 0;
}