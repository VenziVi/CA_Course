// 15. Да се напише програма, която намира сумата на всички числа между 100 и 200,
// които се делят на 9.

#include <stdio.h>

int main() {
    
   int i, sum = 0;

    for ( i = 100; i <= 200; i++)
    {
        if (i % 9 == 0)
            sum += i;
    }
    
    printf("\n Result = %d", sum);
}